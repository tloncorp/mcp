/-  mcp
^-  template:resource:mcp
:*  'scry://ct/{desk}/{case}/{+clayPath}'
    'arvo/clay/list-files'
    `'List Clay files'
    `'Recursively list files below a path in a Clay desk at an explicit case.'
    `'application/json'
    ~
    `[~['agent'] ~ ~]
==
