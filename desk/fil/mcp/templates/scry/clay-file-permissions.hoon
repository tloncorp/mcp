/-  mcp
^-  template:resource:mcp
:*  'scry://cp/{desk}/{case}/{+clayPath}'
    'arvo/clay/permissions'
    `'Read Clay permissions'
    `'Read inherited file or directory permissions from a Clay desk at an explicit case.'
    `'application/json'
    ~
    `[~['agent'] ~ ~]
==
