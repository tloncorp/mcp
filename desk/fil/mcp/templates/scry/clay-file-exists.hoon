/-  mcp
^-  template:resource:mcp
:*  'scry://cu/{desk}/{case}/{+clayPath}'
    'arvo/clay/file-exists'
    `'Check Clay file existence'
    `'Check whether a file exists in a Clay desk at an explicit case.'
    `'application/json'
    ~
    `[~['agent'] ~ ~]
==
