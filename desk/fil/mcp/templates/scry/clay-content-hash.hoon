/-  mcp
^-  template:resource:mcp
:*  'scry://cz/{desk}/{case}/{+clayPath}'
    'arvo/clay/content-hash'
    `'Read Clay content hash'
    `'Read the recursive content hash of a Clay file or directory at an explicit case.'
    `'application/json'
    ~
    `[~['agent'] ~ ~]
==
