/-  mcp
^-  template:resource:mcp
:*  'scry://cx/{desk}/{case}/{+clayPath}'
    'arvo/clay/read-path'
    `'Read Clay filepath'
    `'Read a filepath from a Clay desk at an explicit case.'
    `'text/plain'
    ~
    `[~['agent'] ~ ~]
==
